/*eslint no-console:0*/
'use strict';

var _ = require('lodash');
var urlUtils = require('./urlUtils');
var log = console.log.bind(console);
var warn = console.warn.bind(console);
var BASE_DEPRECATION_MESSAGE = 'DocumentServices|Deprecated|';

var debugEnabled = isDebug();
var verbose = debugEnabled ? log : _.noop;
var warnVerbose = debugEnabled ? warn : _.noop;

function isDebug() {
    return typeof window === 'undefined' ? true : urlUtils.parseUrl(_.get(window, ['location', 'href'], '')).query.debug === 'all';
}

module.exports = {
    isDebug: isDebug,
    verbose: verbose,
    info: log,
    warnVerbose: warnVerbose,
    warn: warn,
    error: console.error.bind(console),
    warnDeprecation: function warnDeprecation(deprecationMessage) {
        if (debugEnabled) {
            console.error.call(console, BASE_DEPRECATION_MESSAGE + deprecationMessage);
        }
    }
};
//# sourceMappingURL=log.js.map